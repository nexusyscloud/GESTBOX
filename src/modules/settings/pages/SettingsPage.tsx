import React from 'react';

/**
 * Configuración básica del sistema (placeholder funcional).
 *
 * Este módulo se mantendrá como punto de entrada a configuraciones futuras:
 * - empresa
 * - usuarios
 * - roles
 * - preferencias
 *
 * Por ahora solo muestra estructura base limpia.
 */

const Settings: React.FC = () => {
  return (
    <div className="space-y-4">
      <h1 className="text-2xl font-bold text-gray-900">Configuración</h1>
      <p className="text-gray-600">
        Módulo de configuración en construcción.
      </p>
    </div>
  );
};

export default Settings;
